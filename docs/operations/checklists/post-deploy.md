# Post-deploy verification

- [ ] OAuth: sign in with Google / Microsoft / X from production SPA.
- [ ] Email/password: sign-in syncs session (`POST /api/auth/supabase/sync`).
- [ ] `GET /api/auth/me` with credentials returns user after login.
- [ ] Logout clears session cookie.
