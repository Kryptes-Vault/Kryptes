# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment secrets (never commit — use Vercel/Render/Supabase dashboards)
.env
.env.*
backend/.env
!.env.example

# Local Google service account JSON (never commit real keys)
kryptes-storage-bot.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ephemeral clone for syncing `docs/` to the MD-archive repo
.md-archive-sync
